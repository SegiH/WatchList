import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.segihovav.watchlist',
  appName: 'watchlist',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
